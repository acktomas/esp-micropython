## 1️⃣分场景使用技巧（Prompt 模板 + 提问技巧）

### 1. 引脚配置场景

```
我正在使用ESP32-S3-DevKitC-1主控板（乐鑫官方版），基于microPython 1.25开发，需要配置以下引脚：
- GPIO18：输出模式，控制LED灯
- GPIO19：输入模式，读取按键状态（上拉电阻）
- GPIO20：ADC模式，读取光敏传感器数据
要求：
1. 生成完整的microPython代码，包含引脚初始化、基础读写函数
2. 代码注释为中文，适配ESP32-S3的引脚特性（避免使用禁用引脚）
3. 给出代码烧录和测试的步骤
```

#### 提问技巧

- 明确说明 ESP32-S3 的开发板型号（避免通用引脚映射错误）；
- 注明引脚用途 + 电气特性（输入 / 输出 / ADC/PWM）；
- 要求 AI “标注禁用引脚”，避免使用 ESP32-S3 的启动引脚（如 GPIO0、GPIO1）。

### 2. 传感器数据读取场景

```
我需要在ESP32-S3-DevKitC-1上用microPython读取DHT11温湿度传感器，传感器连接在GPIO21引脚：
1. 生成完整代码，包含传感器初始化、数据读取、异常处理（如传感器未响应）
2. 要求每隔2秒读取一次数据，并通过串口打印
3. 说明代码运行前的依赖安装（如是否需要导入dht库）
4. 给出常见报错（如Checksum error）的解决方法
```

#### 提问技巧

- 明确传感器型号 + 连接引脚 + 通信协议（I2C/SPI/ 单总线）；
- 要求 AI 包含**异常处理代码**（如传感器断开、数据校验失败）；
- 新手可追加 “用通俗的语言解释代码中每个函数的作用”。

### 3. WiFi/BLE 通信场景

```
我需要在ESP32-S3-DevKitC-1上用microPython实现以下功能：
1. WiFi功能：连接到WiFi（SSID：MyWiFi，密码：12345678），每隔5秒向http://192.168.1.100:8080发送温湿度数据（模拟数据即可）
2. BLE功能：作为从机，广播设备名称“ESP32-S3-Test”，支持手机APP读取温湿度数据
要求：
1. 生成完整代码，包含WiFi重连机制、BLE数据封装
2. 适配ESP32-S3的低功耗WiFi模式
3. 说明代码运行时的注意事项（如端口占用、IP地址配置）
```

#### 提问技巧

- 明确 WiFi/BLE 的具体用途（连接 / 广播 / 数据传输）；
- 要求 AI 加入**容错机制**（如 WiFi 断开自动重连、BLE 连接超时）；
- 注明 “ESP32-S3” 避免 AI 生成 ESP8266 的兼容代码（两者 WiFi 驱动不同）。

### 4. 代码调试排错场景

```
我在ESP32-S3上运行以下microPython代码时出现报错，代码和报错信息如下：
【代码】
import machine
led = machine.Pin(0, machine.Pin.OUT)
led.on()
【报错信息】
OSError: [Errno 19] ENODEV
要求：
1. 解释报错原因（针对ESP32-S3的硬件特性）
2. 给出修复后的完整代码
3. 说明如何避免同类错误（如引脚选择、初始化顺序）
```

#### 提问技巧

- 完整粘贴**代码 + 报错信息 + 运行环境**（如 Thonny 版本、microPython 版本）；
- 要求 AI“先解释错误原因，再给修复代码，最后总结避坑点”；
- 若报错无明确信息，可描述现象（如 “LED 不亮但无报错”），让 AI 分析可能原因。

### 5. 低功耗优化场景

```
我需要优化ESP32-S3的microPython代码，实现低功耗运行：
1. 功能背景：每隔10秒读取DHT11数据，通过BLE发送，其余时间休眠
2. 要求：
   - 关闭未使用的外设（WiFi、串口、ADC）
   - 使用ESP32-S3的深度休眠模式（RTC定时器唤醒）
   - 生成完整代码，标注功耗优化的关键代码行
   - 给出功耗测试的方法（如串口打印电流值）
```

#### 提问技巧

- 明确休眠触发条件（定时 / 外部中断）和唤醒方式；
- 要求 AI 标注**低功耗关键代码**（如`machine.deepsleep()`的使用）；
- 追加 “说明不同休眠模式（浅休眠 / 深度休眠）的功耗差异和适用场景”。



## 2️⃣ 分场景 Prompt 模板（直接复制即用）

> 使用口诀：**“给角色+限硬件+提需求+要注释”**，AI 生成一次到位率能到 90%。

### ① 引脚配置 & 外设初始化

```text
角色：你是 microPython 专家，熟悉 ESP32-S3 GPIO 矩阵。
硬件限制：ESP32-S3，microPython v1.25，仅 512 KB SRAM。
任务：
1) 配置 GPIO18、GPIO19 为 I²C，频率 400 kHz；
2) 扫描总线上所有从设备地址并打印；
3) 加入 try/except，若无设备给出中文提示。
输出：完整 main.py + 中文行注释，变量名英文。
```

### ② 传感器数据读取

```text
场景：BME280 已接在刚才的 I²C 总线，地址 0x76。
要求：
- 使用官方 bme280.py 库，软校准海拔 50 m；
- 每 5 秒采集一次温度/湿度/气压；
- 串口输出 JSON 格式，方便后续 Node-RED 解析；
- 内存占用 <8 KB，给出 gc.mem_free() 实测值。
输出：main.py + 所需库文件列表。
```

### ③ Wi-Fi/BLE 配网

```text
需求：
- 建立一个 Web 配网页，首次开机启动 AP，名 "ESP32_xxxx"；
- 用户在网页输入 Wi-Fi 账号密码后，自动连接并记忆到 NVS；
- 配网成功熄灭板载 LED，失败重新进入 AP 模式。
输出：完整代码 + 目录结构说明，指出需放入的 html 文件位置。
```

### ④ 异常调试 & 日志

```text
问题：运行中随机出现 "E (12345) task_wdt: Task watchdog got triggered"。
请给出：
1) 可能 3 种 root cause（中文）；
2) 一段可在 REPL 执行的诊断脚本，打印当前任务栈、CPU 频率、看门狗剩余时间；
3) 对应的 3 段修复代码片段，分别对应 3 种原因。
代码需兼容 microPython，不含 ESP-IDF 宏。
```

### ⑤ 低功耗优化

```text
目标：让 ESP32-S3 用电池运行 ≥3 个月，每天仅上传 24 条数据。
已知：深度睡眠电流 8 µA，唤醒 120 mA@150 ms，ADC 采集 2 s。
请输出：
1) 电源模式配置脚本（lightsleep/deepsleep 时机）；
2) 使用 ULP 协处理器定时唤醒的 microPython 示例（伪代码亦可）；
3) 计算电池寿命的公式及参数说明。
```

------

## 3️⃣三、避坑指南

### 1. 常见误区及规避方法

|                 常见误区                 |                           具体风险                           |                           规避方法                           |
| :--------------------------------------: | :----------------------------------------------------------: | :----------------------------------------------------------: |
| 直接使用 AI 生成的代码，不验证引脚兼容性 | ESP32-S3 部分引脚（如 GPIO0、GPIO1）为启动引脚，误用会导致硬件无法启动 | 1. 向 AI 提问时明确要求 “标注 ESP32-S3 禁用引脚”；2. 先在 Wokwi 仿真验证引脚配置，再烧录硬件。 |
|    忽略 microPython/ESP-IDF 版本差异     | AI 生成的代码可能基于旧版本，导致函数报错（如`network.WLAN`参数变化） | 1. 提问时注明 “microPython 1.25 + ESP-IDF v5.1”；2. 要求 AI 生成代码时标注 “兼容版本范围”。 |
|  信任 AI 生成的硬件调试结论，不核对手册  | AI 可能给出错误的硬件参数（如 ADC 量程、PWM 频率），导致传感器读取数据异常 | 1. 关键参数（如 ADC 参考电压）需核对 ESP32-S3 官方手册；2. 向 AI 提问时附上手册关键片段，要求基于手册生成代码。 |
|         代码无异常处理，直接上线         | AI 生成的代码通常无容错逻辑，硬件环境变化（如传感器断开）会导致程序崩溃 | 1. 要求 AI 在代码中加入`try-except`异常处理；2. 关键功能（如 WiFi 连接）增加重试机制。 |
|       过度依赖 AI，不理解核心逻辑        |          仅复制代码不理解原理，后续调试 / 修改困难           | 1. 每次提问追加 “用通俗语言解释核心函数的作用”；2. 让 AI 分步生成代码，先理解框架再补充细节。 |

### 2. 额外注意事项

- AI 生成的代码可能缺少 “导入库” 语句（如`import dht`），需检查代码开头；
- ESP32-S3 的部分功能（如 USB 通信）在 microPython 中支持度有限，需提前向 AI 确认 “该功能是否可在 ESP32-S3 的 microPython 中实现”；
- 免费版 AI 模型可能生成重复 / 冗余代码，可要求 AI “精简代码并保留核心功能”。
